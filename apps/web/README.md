# e-list web (frontend scaffold)

This folder is a minimal Next.js + Tailwind scaffold for starting the frontend work.

To run locally:

```bash
cd apps/web
npm install
npm run dev
```

The page `http://localhost:3000` contains a simple hero and product cards matching the provided design.
